import Image from 'next/image';
import styles from '../styles/Footer.module.css';

const Footer = () => {
  return (
    <div className={styles.container}>
      <p className={styles.header}>Hanoon Cosmetics Inc</p>
    </div>
  );
};

export default Footer;
